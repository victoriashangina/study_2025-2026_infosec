# Простой Makefile для лабораторной работы
SRC = Л01_Шангина_отчёт.md
PDF = Л01_Шангина_отчёт.pdf
DOCX = Л01_Шангина_отчёт.docx

all: pdf docx
	@echo "✅ Готово!"

pdf:
	@echo "🔨 Создаю PDF файл..."
	pandoc $(SRC) -o $(PDF)
	@echo "✅ PDF создан: $(PDF)"

docx:
	@echo "🔨 Создаю DOCX файл..."
	pandoc $(SRC) -o $(DOCX)
	@echo "✅ DOCX создан: $(DOCX)"

clean:
	rm -f $(PDF) $(DOCX)
	@echo "🧹 Очистка завершена"
